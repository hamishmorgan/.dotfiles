# Backup directories
backups/

# Temporary files and proposals (not committed to git)
tmp/

# Machine-specific configuration files (contain sensitive/private information)
*.local
.gitconfig.local
.bashrc.local
.zshrc.local
.bash_profile.local
.zprofile.local
config_private.fish
/.claude/settings.local.json
