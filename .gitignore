# Backup directories
backups/

# Secret configuration files (contain sensitive information)
*.secret
*.local

# Specific secret files
git/.gitconfig.secret
git/.gitconfig.shopify.secret
gh/.config/gh/config.yml.secret
gh/.config/gh/hosts.yml.secret

# Generated configuration files (created from templates + secret configs)
git/.gitconfig
git/.gitconfig.shopify
gh/.config/gh/config.yml
gh/.config/gh/hosts.yml
