# .env.local.example
# Copy to .env.local for local environment variables (git-ignored)
#
# These variables are loaded automatically by direnv when present
# Format: KEY=value (no export needed, direnv handles that)

# Example:
# CI_ARGS=--verbose
# PLATFORM=alpine
# STOW_FLAGS=--verbose=3
