// Place your key bindings in this file to override the defaults
[
    {
        "key": "cmd+i",
        "command": "composerMode.agent",
        "when": "isMac"
    },
    {
        "key": "ctrl+i",
        "command": "composerMode.agent",
        "when": "!isMac"
    }
]
