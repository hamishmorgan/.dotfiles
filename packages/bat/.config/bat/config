# bat configuration
# https://github.com/sharkdp/bat

# Theme
--theme="TwoDark"

# Display components (numbers, changes, header)
--style="numbers,changes,header"

# Paging behavior (auto = use pager for large output)
--paging=auto

# Text wrapping (never = no line wrapping)
--wrap=never

# Tab width
--tabs=4

# Syntax mappings for common file types
--map-syntax="*.conf:INI"
--map-syntax=".ignore:Git Ignore"
--map-syntax="*.env:Bash"
--map-syntax="Dockerfile*:Dockerfile"
