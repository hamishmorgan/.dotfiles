# macOS-specific fish configuration
# Note: Homebrew setup is in conf.d/brew.fish (needs to run before tec init)
switch $FISH_HOST_OS
    case darwin
        # Add macOS-specific environment variables or aliases here
end
